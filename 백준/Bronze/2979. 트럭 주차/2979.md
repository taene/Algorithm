### 첫번째 풀이
```cpp
#include <iostream>
using namespace std;

int a, b, c;
int arr[102];
int sum;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	cin >> a >> b >> c;

	for (int i = 0; i < 3; i++)
	{
		int x, y;
		cin >> x >> y;
		for (int j = x; j < y; j++)
		{
			arr[j]++;
		}
	}

	for (int i = 1; i <= 100; i++)
	{
		if (arr[i] == 0) continue;
		else if (arr[i] == 1)
			sum += a;
		else if (arr[i] == 2)
			sum += b * 2;
		else if (arr[i] == 3)
			sum += c * 3;
	}

	cout << sum;

	return 0;
}
```

<br/>

### 두번째 풀이
```cpp
#include <iostream>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int parkTime[100] = {};
    int a, b, c;
    cin >> a >> b >> c;
    for (int i = 0; i < 3; ++i)
    {
        int x, y;
        cin >> x >> y;
        for (int j = x; j < y; ++j)
        {
            parkTime[j]++;
        }
    }

    int sum = 0;
    for (int i = 0; i < 100; ++i)
    {
        switch (parkTime[i])
        {
        case 1:
            sum += a;
            break;
        case 2:
            sum += b * 2;
            break;
        case 3:
            sum += c * 3;
            break;
        default:
            break;
        }
    }
    cout << sum;

    return 0;
}
```
